@if (Event is not null)
{
    <BrowserLink href=@LinkUrl OnClick="OnClickInternal" AdditionalAttributes="@AdditionalAttributes">
        @if (ChildContent is not null)
        {
            @ChildContent
        }
        else

        {
            @Label
        }
    </BrowserLink>
}