@if (Event is not null)
{
    <button type="button" class="btn btn-link p-0" @attributes="@AdditionalAttributes" @onclick="DownloadEvent">
        @if (ChildContent is not null)
        {
            @ChildContent
        }
        else
        {
            @Label
        }
    </button>
}