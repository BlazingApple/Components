@using Syncfusion.Blazor.Popups;

<SfTooltip Width="@WidthStyle" CssClass="@($"e-tooltip-css {AdditionalClasses}")" OpensOn="@OpenOn.ToString()" Target="@TargetId" CloseDelay="@CloseDelay">
	<TooltipTemplates>
		<Content>
			@TooltipContent
		</Content>
	</TooltipTemplates>
	@ChildContent
</SfTooltip>
